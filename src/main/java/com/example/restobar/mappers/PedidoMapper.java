package com.example.restobar.mappers;

import com.example.restobar.dtos.request.PedidoRequest;
import com.example.restobar.models.Pedido;
import org.springframework.stereotype.Service;

@Service
public class PedidoMapper {

    /*public Pedido productoRequestToProducto(PedidoRequest pedidoRequest){
        Pedido producto = Pedido.builder()
                .mesa(pedidoRequest.getMesa())
                .descripcion(pedidoRequest.getDescripcion())
                .costo(pedidoRequest.getCosto())
                .precio(pedidoRequest.getPrecio())
                .build();
        return producto;
    }*/
}
